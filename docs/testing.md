# Testing the bootstrap

Recommended rehearsal:

1) Create a new macOS user (System Settings → Users & Groups).
2) Log into the new user.
3) Run the “Quick start” from `README.md`.
4) Verify zsh starts cleanly and Warp config exists under `~/.warp/`.

